import React from 'react';
import TableRules from "../components/TableRules.tsx";
// Import the Table component for displaying device data

// Dashboard component that manages the layout of the page, including the sidebar and main content
const Rules: React.FC = () => {
    // State to manage the collapsed/expanded state of the sidebar

    return (
        <>
            <TableRules/>
        </>
    );
};

export default Rules; // Export the Dashboard component for use in other parts of the app
